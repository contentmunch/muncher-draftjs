import {Meta} from '@storybook/addon-docs/blocks';

<Meta title="Introduction"/>

# Muncher
[Muncher](https://muncher.contentmunch.com/) is a rich text editor based on [Draftjs](https://draftjs.org/) with a focus on content reuse.

## Muncher Component Explorer
Browse stories by navigating them in the sidebar, view code in the **Docs** view.
Follow [Muncher-UI](https://muncher-ui.contentmunch.com/) for UI components used in Muncher.

## Using Muncher
To use this component library:

* add the npm package to your project
    ```
    $ npm install @contentmunch/muncher
    ```
## Installation guide
In the project directory, you can run:

### `npm start`
Launches the component explorer on port 6009

### `npm test`

Launches the test runner in the interactive watch mode.<br />
See the section about [running tests](https://facebook.github.io/create-react-app/docs/running-tests) for more information.

### `npm run build`

Builds the package library.



